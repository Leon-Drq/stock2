POSTGRES_DB=stock2
POSTGRES_USER=stock2

# Use a long generated password in production.
POSTGRES_PASSWORD=replace-with-postgres-password

# Expose PostgreSQL only when local tools need direct access.
POSTGRES_PORT=5432
